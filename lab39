
import numpy as np
a = np.array([
    [3.0, 4.0],
    [1.0, 2.0],
    [0.0, 5.0],
])
b = np.sqrt(np.sum(a**2, axis=1))
c = b.reshape(-1, 1)
d = a / c
e = np.sqrt(np.sum(d**2, axis=1))
print(a)
print( b)
print( d)
print( e)
